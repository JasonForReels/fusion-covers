{
  "name": "fusion-covers-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "db:local": "wrangler d1 execute fusion-covers-sync --local --file=schema.sql",
    "db:remote": "wrangler d1 execute fusion-covers-sync --remote --file=schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
